# Token 使用分析報告
**生成時間：** 2026-02-03 03:58 UTC  
**分析範圍：** 最近 659 條對話記錄

---

## 📊 使用摘要（最近 38 次互動）

| 項目 | 數值 |
|------|------|
| 總互動次數 | 38 次 |
| 總 Input Tokens | 436 (平均 11/次) |
| 總 Output Tokens | 18,907 (平均 498/次) |
| **總成本** | **$1.97** |
| **平均成本/次** | **$0.052/次** |

---

## 💰 成本分布（Claude Sonnet 4.5 價格）

### Token 類型成本結構
- **Input tokens**: $3/1M tokens（最便宜）
- **Output tokens**: $15/1M tokens（**最貴**）
- **Cache read**: $0.15/1M tokens（超便宜）
- **Cache write**: $3.75/1M tokens（中等）

### 實際使用模式
每次互動平均消耗：
- Input: ~11 tokens (Chris 的短訊息)
- Output: ~498 tokens (我的回覆)
- Cache read: ~150,000 tokens（重複載入系統 prompt）
- Cache write: ~500-5,000 tokens（更新 context）

**關鍵發現：**
- ✅ Cache 機制運作良好，節省 95%+ 成本
- ⚠️ Output tokens 是主要成本來源（佔 40-60%）
- ⚠️ 長回覆（>3000 tokens）導致單次成本飆升至 $0.10+

---

## 🔥 Top 10 最昂貴的互動

| 排名 | Output Tokens | 成本 | 時間 |
|------|---------------|------|------|
| 1 | 5,399 | $0.12 | 2026-02-03 03:40 |
| 2 | 5,161 | $0.10 | 2026-02-03 00:24 |
| 3 | 5,086 | $0.10 | 2026-02-03 00:14 |
| 4 | 4,784 | $0.12 | 2026-02-02 19:05 |
| 5 | 4,560 | $0.11 | 2026-02-02 07:42 |
| 6 | 4,547 | $0.11 | 2026-02-03 03:33 |
| 7 | 4,415 | $0.09 | 2026-02-02 23:51 |
| 8 | 4,406 | $0.10 | 2026-02-03 00:28 |
| 9 | 4,299 | $0.09 | 2026-02-03 00:23 |
| 10 | 4,248 | $0.09 | 2026-02-03 00:26 |

**分析：**
- 前 10 名都發生在實作期間（market-digest / security-patrol）
- 這些是「長程式碼輸出」或「詳細說明」場景
- 單次最高成本 $0.12（約佔平均成本的 2.3 倍）

---

## 🎯 優化建議（按影響力排序）

### ⭐ 高影響力（立即實施）

#### 1. **縮減系統 Prompt**
**問題：** 系統 prompt 佔 18,724 字元，每次都要 cache
- Tools schema: 13,981 字元（最大部分）
- Project context: 8,323 字元
- Skills: 2,314 字元

**建議：**
```bash
# 移除不常用的 skills
- ❌ bluebubbles (323 chars) - 從未使用
- ❌ notion (256 chars) - 從未使用
- ❌ skill-creator (324 chars) - 低頻使用

# 簡化 BOOTSTRAP.md
- 目前 1,444 字元，已完成初始化，可刪除

# 預估節省：~2,500 字元（13% 系統 prompt）
```

**執行方式：**
```bash
# 1. 刪除 BOOTSTRAP.md（已完成初始化）
rm ~/clawd/BOOTSTRAP.md

# 2. 在 clawdbot.json 移除不常用 skills
# （需手動編輯 config）
```

#### 2. **切換到 Thinking Mode: Off**
**問題：** 目前 thinking=low，每次回覆都包含思考過程
- Thinking tokens 也計入 output（$15/1M）
- 日常互動不需要詳細思考過程

**建議：**
```bash
# 關閉 thinking（僅在複雜問題時手動開啟）
/reasoning off

# 或設定為「只在需要時開啟」
```

**預估節省：** 10-20% output tokens

#### 3. **精簡日常回覆**
**問題：** 平均 output 498 tokens，部分回覆過於詳細
- 確認訊息可用 emoji 代替（✅/⚠️/📊）
- 狀態報告可壓縮為表格
- 避免重複說明已知資訊

**建議範例：**
```
❌ 原本（120 tokens）：
「已完成巡邏。檢查了 8 個項目，包括 SSH、更新、防火牆...
沒有發現異常。系統運作正常。」

✅ 優化（30 tokens）：
「✅ 巡邏完成 | 8 項檢查 | 0 異常」
```

**預估節省：** 20-30% output tokens

---

### 🟡 中影響力（逐步實施）

#### 4. **使用更便宜的模型處理簡單任務**
**問題：** 所有任務都用 Sonnet 4.5（$15/1M output）
- 簡單確認、狀態查詢不需要最強模型

**建議：**
```bash
# 在 clawdbot.json 設定 routing rules
{
  "modelRouting": {
    "simple": "haiku-4",      # $1.25/1M output (便宜 12 倍)
    "default": "sonnet-4.5"
  }
}
```

**適合用 Haiku 的場景：**
- ✅ 狀態查詢（/status, session_status）
- ✅ 簡單確認（「好」、「執行」）
- ✅ 巡邏報告（已格式化資料）
- ✅ 日常推播（早報整合）

**預估節省：** 30-40% 總成本（若 50% 任務改用 Haiku）

#### 5. **實作指令化互動**
**問題：** 每次都用自然語言，浪費 tokens
- 「查看今天的早報」vs `/today`
- 「執行巡邏」vs `/patrol`

**建議：**
```bash
# 在 AGENTS.md 新增指令表
## 快速指令
/today - 查看早報
/patrol - 執行巡邏
/status - 系統狀態
/watchlist - 個股追蹤

# 優點：
- 減少解析成本
- 更快回應
- 降低誤判
```

**預估節省：** 15-20% input + output tokens

#### 6. **拆分長回覆為多則訊息**
**問題：** 單次 5000+ tokens 導致成本飆升
- Telegram 支援多則訊息
- 使用者體驗也更好（逐步呈現）

**建議：**
```javascript
// 範例：實作報告分段推送
async function sendLongReport(report) {
  const sections = report.split('━━━'); // 按分隔符切段
  for (const section of sections) {
    await sendMessage(section);
    await sleep(500); // 避免 rate limit
  }
}
```

**預估節省：** 不直接節省成本，但改善體驗 + 避免 timeout

---

### 🟢 低影響力（選用）

#### 7. **啟用 Summary 壓縮**
**問題：** 659 行對話持續累積，context 越來越大
- 目前只有 2% context 使用率（22k/1M）
- 但長期會增加 cache write 成本

**建議：**
```bash
# 定期壓縮對話歷史（保留重要資訊）
# Clawdbot 內建 compaction 機制，已自動啟用
```

**目前狀態：** ✅ 已有 summary 機制，無需調整

#### 8. **監控異常消耗**
**建議：**
```bash
# 建立每日成本監控
cat > ~/clawd/agents/cost-monitor.sh << 'EOF'
#!/bin/bash
# 每天統計 token 使用
tail -200 ~/.clawdbot/agents/main/sessions/*.jsonl | \
  jq -r 'select(.message.usage != null) | .message.usage.cost.total' | \
  awk '{sum+=$1; count++} END {printf "今日成本: $%.4f (%d 次互動)\n", sum, count}'
EOF

chmod +x ~/clawd/agents/cost-monitor.sh

# 加入 cron（每天 23:59 推播）
```

---

## 📈 預估總節省

實施**高影響力**措施（1-3）：
- 縮減系統 prompt: -13%
- 關閉 thinking: -15%
- 精簡回覆: -25%
- **合計節省：約 40-50% 總成本**

加上**中影響力**措施（4-5）：
- 改用 Haiku 處理簡單任務: -35%
- **合計節省：約 60-70% 總成本**

### 成本預測
- 目前：$0.052/次 → **$1.56/月**（30 次/天）
- 優化後：$0.020/次 → **$0.60/月**（30 次/天）
- **節省：$0.96/月（62%）**

---

## ✅ 立即行動清單

1. **刪除 BOOTSTRAP.md**（已完成初始化）
2. **關閉 thinking mode**（`/reasoning off`）
3. **更新 SOUL.md**：加入「精簡回覆」規則
4. **測試 Haiku 路由**：簡單任務改用便宜模型
5. **建立指令表**：減少自然語言解析

需要我立即執行以上優化嗎？
